# 버그 수정 및 기능 개선 구현 계획

버그확인.txt 기반. 각 Step은 독립적으로 커밋 가능한 단위로 설계됨.

---

## Step 1. ROI 설정 팝업 - 배경 클릭으로 닫히지 않도록

**문제**: 박스 그린 후 설정 팝업 뜰 때 바깥 클릭하면 팝업이 사라짐.

**수정**: `js/ui.js` `openRoiSettingsPopup()` 의 background click 리스너 제거.
- **닫는 방법**: 취소 버튼, 확인 버튼, ESC 키만 허용
- ESC 키 처리는 `js/shortcuts.js`에 `#roi-settings-popup` 모달이 열려있으면 취소 처리

**파일**: `js/ui.js`, `js/shortcuts.js`

---

## Step 2. numQuestions 0 버그 수정

**문제**: 사이드패널에서 문항수가 0으로 표시되고 무한대로 분석 돌아감.

**원인**: `applyRoiSettingsPopup()`와 `setOrientation()`에서 방향 변경 시 `numQuestions = 0`으로 리셋. 이후 자동감지에 의존하는데 실패하면 0인 채로 남음.

**수정**:
- 방향 변경 시 0으로 리셋하지 말고, 기존 값 유지
- 자동감지는 **초기 박스 생성 시에만** 동작
- 사용자가 이미 입력한 값은 덮어쓰지 않음

**파일**: `js/ui.js`

---

## Step 3. 과목 그룹핑 시스템 도입

**문제**: "1번~25번" 같은 시험에서 영역 3개(1-10, 11-20, 21-25)로 나눌 때 각 영역이 독립적이라 과목으로 묶을 방법이 없음.

**제안**:
- **과목(Subject)** 개념을 ROI 레벨에 추가
- 각 ROI가 `subjectName` 필드를 가짐 (예: "영어")
- 같은 `subjectName`을 가진 ROI들은 하나의 과목으로 묶여 채점됨
- 정답은 과목 단위로 관리 (과목별 정답 입력)
- UI: ROI 카드 상단에 "과목" 드롭다운 (기존 과목 선택 + "새 과목" 추가)

**데이터 구조**:
```javascript
// defaultSettings에 추가
subjectName: '',  // 과목명 (빈 문자열이면 독립 ROI)

// App.state에 추가
subjectAnswers: {
    '영어': { answers: [1,2,3,...], numChoices: 5 },
    '수학': { answers: [3,1,4,...], numChoices: 5 }
}
```

**채점**:
- `grading.js`에서 ROI의 `subjectName`으로 해당 과목의 답 배열을 가져옴
- ROI의 `startNum`을 과목 내 오프셋으로 사용

**파일**: `js/ui.js`, `js/grading.js`, `js/app.js`

---

## Step 4. 선택지 직접 입력 방식으로 전면 개편

**문제**: 현재 프리셋(1-5, A-E 등) 중심. 한글/특수문자 등 자유 입력이 불편.

**제안 방식**:
```
1. 선택지 개수: [5]  ← 숫자 입력
2. 선택지: [1] [2] [3] [4] [5]  ← 개수만큼 텍스트박스 자동 생성
```

- `numChoices` 값에 따라 텍스트박스가 동적으로 생성됨
- 각 텍스트박스는 해당 인덱스의 `choiceLabels[i]` 값
- 사용자가 자유롭게 수정 가능 (예: "ㄱ", "ㄴ", "ㄷ", "ㄹ", "ㅁ")
- 기존 `CHOICE_PRESETS`는 **"빠른 설정" 드롭다운**으로 유지 (선택 시 텍스트박스들이 해당 값으로 채워짐)

**파일**: `js/ui.js`, `js/grading.js` (채점 로직도 라벨 기반으로 수정)

---

## Step 5. 길쭉 버블 슬라이더 → 일반 버블 분석에도 적용

**문제**: 슬라이더가 길쭉 모드 전용. 기본 모드에서도 h/w, fill 같은 임계값 조정이 필요할 수 있음.

**수정**:
- 슬라이더 섹션을 **"버블 감지 설정"** 으로 헤더 변경
- **항상 표시**(체크박스 없이 항상 펼쳐짐)
- 슬라이더 항목:
  - **h/w 하한** (기본 0.7 = 원형, 1.4 = 약간 길쭉, 2.0 = 매우 길쭉)
  - **h/w 상한** (기본 5.0)
  - **채움 하한** (기본 0.15)
  - **채움 상한** (고정 1.0, 슬라이더 없음)
- 엔진: `_stage1_findBubbles`와 `_stage1_findBubbles_elongated`를 **하나로 통합**
  - 파라미터로 `minHW`, `maxHW`, `minFill` 받음
  - `minHW < 1.0`이면 기존 로직 (w/h 기반), `minHW >= 1.0`이면 h/w 기반
  - 또는 더 깔끔하게: 항상 h/w 기반으로 통일, 원형은 `minHW=0.7~1.4` 범위

**팝업+사이드바 양쪽 설정**: 최초 박스 생성 팝업에도, 나중에 카드에도 동일 슬라이더 노출.

**파일**: `js/ui.js`, `js/omrEngine.js`, `js/canvas.js`

---

## Step 6. 길쭉 버블 일괄채점 (Batch) 지원

**문제**: `js/batch.js`의 `analyzeROI()` 호출에 `elongatedMode`, `elongatedThresholds` 파라미터 누락. 일괄처리 시 길쭉 모드 설정이 무시됨.

**수정**: `js/batch.js`에서도 `canvas.js`의 `runAnalysis()`와 동일한 방식으로 설정을 읽어서 전달.

**파일**: `js/batch.js`

---

## Step 7. 균등 그리드 생성 + 가로 스트레칭 전면 폐기

**제거 대상**:
- `createManualGrid()`, `clearGrid()` (ui.js)
- "균등 그리드 생성" 버튼 UI
- `stretchRatio`, `showStretchPreview` 설정 필드
- `onStretchChange()`, `onPreviewChange()` 핸들러
- 스트레칭 관련 canvas 렌더링 코드 (미리보기 오버레이)
- `runAnalysis()`의 스트레칭 분기 로직 (canvas.js 440-483)

**파일**: `js/ui.js`, `js/canvas.js`

---

## Step 8. 회전 개별 적용 지원

**문제**: `rotateImage()`가 전체 이미지 대상. 개별 적용 불가.

**수정**:
- `rotateImage(degrees, applyAll)` 로 파라미터 추가
- 진하기/기울기와 동일하게 **"전체 적용"** 체크박스 추가
- 체크 OFF: 현재 이미지만, ON: 전체 이미지

**파일**: `js/canvas.js`, `index.html`

---

## Step 9. 이미지 조정바 UI 통일

**문제**: 조정바의 버튼들이 "전체/개별" 처리 방식이 제각각.

| 현재 | 진하기 | 기울기 | 회전 |
|------|-------|-------|------|
| 개별 적용 | 슬라이더 직접 조정 | 체크박스 OFF | **없음** |
| 전체 적용 | 별도 버튼 | 체크박스 ON | 버튼 누르면 무조건 전체 |

**제안 (통일 방식)**:
```
[진하기 슬라이더] [□ 전체 적용]
[기울기 -] [기울기 +] [□ 전체 적용]
[좌회전] [우회전] [□ 전체 적용]
[원상복구]
```
- 모든 조정에 **동일한 "전체 적용" 체크박스** 패턴
- "전체 적용" 체크되어있으면 해당 조정은 모든 이미지에
- 체크 해제되어있으면 현재 이미지만
- 기존 "전체 적용" 버튼 제거 (체크박스 + 버튼은 일관성 깨뜨림)

**파일**: `js/canvas.js`, `index.html`

---

## Step 10. 전체 점검 + 추가 버그 찾기

Step 1~9 완료 후:
- 통합 테스트: 템플릿 저장/로드, 배치 처리, 채점 결과
- 수동 테스트 항목:
  - 영역 그리기 → 설정 팝업 → ESC/취소/확인
  - 영역 변경(순서, 방향) 시 numQuestions 유지
  - 과목 설정 → 여러 영역 연결 → 채점 정확성
  - 선택지 직접 입력 → 한글 OMR 시뮬레이션
  - 길쭉 버블 + 배치
  - 회전 개별/전체
- 콘솔 에러, 예외 처리 누락 확인

---

## Step 11. 사용자 확인 후 구현 시작

이 md 파일에 동의하시면 **Step 1부터 순서대로** 구현합니다.

---

## 구현 시 주의사항

1. **각 Step마다 독립 커밋** — 문제 생기면 개별 롤백 가능
2. **기존 동작 영향 최소화** — 특히 분석 엔진, 채점 로직은 신중히
3. **테스트 이미지 기반 검증** — 기존에 잘 되던 OMR은 계속 잘 되어야 함
4. **7번(제거) 먼저 하는 게 안전** — 중복 로직 줄이고 이후 작업 단순화

---

## 질문 및 확인 필요 사항

1. **Step 3 (과목)**: 과목을 ROI 레벨 필드로 관리하는 방식 괜찮으신지? 아니면 별도 "과목 매니저" 탭이 필요한지?

2. **Step 4 (선택지 직접 입력)**: 프리셋 드롭다운을 유지하여 빠른 설정 가능하게 할지, 아니면 완전히 제거하고 직접 입력만 남길지?

3. **Step 5 (슬라이더 통합)**: `_stage1_findBubbles`와 `_stage1_findBubbles_elongated`를 하나로 합칠지, 아니면 두 개 유지하고 UI에서만 통합할지?

4. **Step 9 (조정바 통일)**: 제안한 "전체 적용 체크박스" 패턴 괜찮은지?

5. **구현 순서**: Step 번호대로 진행할지, 아니면 다른 순서 선호하시는지?

답변 주시면 바로 Step 1부터 시작하겠습니다.
