# 교시별 채점 기획서

## 📌 문제 정의

### 현재 한계
- 한 세션 = 한 OMR 양식 = 한 번의 시험
- OMR 양식(ROI 구성, 답안키)이 세션 전체에 공유됨 → **교시별로 양식이 다를 수 없음**
- 하루에 두 번 시험 보는 경우, 두 번째 OMR을 첫 번째 양식으로 채점해야 해서 **과목 구분 불가**
- 두 번째 OMR에 맞게 양식을 수정하면 첫 번째 OMR 결과가 **뒤엎힘**

### 원하는 것
- **한 세션 안에서 교시를 여러 개** 운영
- **교시마다 독립된 OMR 양식 / 답안키 / 과목 구성**
- 학생별로 모든 교시 결과를 **자동으로 연결** (수험번호/전화번호 기반)
- 최종 성적표는 **교시 통합**(같은 과목명은 합산, 교시 간 과목 중복 없음 가정)

---

## 🎯 핵심 원칙

1. **한 세션 = 여러 교시** (1교시, 2교시, ..., N교시)
2. **한 교시 = 고유한 OMR 양식** (ROI + 답안키 + 과목 설정)
3. **한 교시 = 여러 이미지** (학생별 OMR 한 장씩)
4. **한 학생 = 교시 수만큼 OMR** 존재 (결시도 허용)
5. **학생 매칭**: 수험번호(우선) → 전화번호 → 이름 (교시 간 학생 연결)
6. **과목 합산**: 같은 과목명은 교시가 달라도 합산 (기존 다과목 로직 그대로 확장)

---

## 📋 케이스 분석

### Case 1: 단일 교시, 단일 과목 (기존 기본 케이스)
- 1교시만 존재, 1 OMR = 1 과목
- 예: 고등학교 중간고사 국어 단일 시험
- 현재 시스템으로도 처리됨 → 1교시 1과목으로 자연스럽게 맵핑

### Case 2: 단일 교시, 다과목 (한 OMR에 여러 과목)
- 1교시만 존재, 1 OMR에 여러 subject_answer ROI
- 예: 공무원 시험(국어+영어+한국사 한 장)
- 현재 시스템으로도 처리됨 → 1교시 다과목

### Case 3: 다교시, 교시당 단일 과목 ⭐ 신규
- N교시 각각 독립 OMR, 교시당 과목 1개
- 예: 로스쿨 시험 (1교시 언어이해, 2교시 추리논증)
- **현재 불가** → 이번 기획의 핵심

### Case 4: 다교시, 교시당 다과목 ⭐ 신규
- N교시 각각 독립 OMR, 교시당 과목 여러 개
- 예: 1교시 국어+영어 (한 장 OMR), 2교시 수학+과학 (다른 OMR)
- **현재 불가** → 이번 기획의 핵심

### Case 5: 교시별 응시자 풀 차이 (선택/결시)
- 1교시 전체 응시, 2교시는 일부만 응시(선택과목 또는 결시)
- 학생 매칭 시 일부 교시에 OMR 없는 학생은 **해당 교시만 공란** 처리
- 성적표에서 해당 과목만 "미응시"로 표기

### Case 6: 같은 과목명이 여러 교시에 걸침 (합산?)
- 1교시 국어 10문항 + 2교시 국어 10문항 → 합산?
- **확정 필요** (아래 "⚠ 확정 필요 사항 참조")

### Case 7: 교시별 같은 OMR 양식 (재사용)
- 두 번의 시험이 같은 양식인 경우, 2교시가 1교시 양식을 복사해서 시작
- **편의 기능** (필수는 아님)

---

## 🏗 데이터 구조 설계

### 세션 (App.state)

```
session = {
  name: '2026 봄 모의고사',
  periods: [
    {
      id: 'p1',
      name: '1교시 (언어이해)',
      images: [img1, img2, ...],        // 이 교시 소속 OMR 이미지들
      answerKey: {...},                  // 이 교시 고유 답안키
      roiTemplate: {...},                // 이 교시 고유 ROI 템플릿(없을 수도 있음)
      subjects: {...}                    // 이 교시의 과목 설정
    },
    {
      id: 'p2',
      name: '2교시 (추리논증)',
      ...
    }
  ],
  currentPeriodId: 'p1',                 // 지금 편집/분석 중인 교시
  students: [...]                        // 세션 전체 응시자 (교시 공유)
}
```

### 이미지
```
image = {
  name, path, imgElement, ...,
  periodId: 'p1',                        // 어느 교시 소속
  rois: [...],                           // 교시별 독립 (교시 간 복사/공유 가능)
  results: [...],
  gradeResult: {...}
}
```

### collectData 출력 (교시 통합)
```
row = {
  examNo, name, birthday, phone,
  subjects: {
    '언어이해': { score, correctCount, ..., period: 'p1' },
    '추리논증': { score, correctCount, ..., period: 'p2' }
  },
  totalScore, totalCorrect, ...,
  _periodMissing: ['p2']                 // 결시한 교시 표기 (옵션)
}
```

---

## 🖼 UI 변화

### 상단 헤더
현재: `[세션명]`
변경: `[세션명] │ [1교시▼] [2교시] [+ 교시 추가]`
- 교시 드롭다운 또는 탭
- 현재 선택된 교시의 이미지/ROI만 보임
- "+ 교시 추가" 버튼으로 새 교시 생성

### 이미지 업로드
- 드래그 앤 드롭 시 **현재 선택된 교시**에 추가됨
- 기존 이미지를 다른 교시로 이동하는 UI도 있어야 함 (우클릭 메뉴 or 드래그)

### ROI 편집기
- 현재 교시의 이미지에 그린 ROI만 그 교시에 적용
- 다른 교시 이미지에는 영향 없음
- **템플릿 복사**: 한 교시의 ROI를 다른 교시로 복사하는 편의 기능

### 분석/채점
- 현재 교시 이미지만 분석/채점
- 또는 "전체 교시 일괄 채점" 버튼

### 채점 결과 (성적 탭)
- OMR 결과표: 교시별 드롭다운 or 통합 보기
- 성적일람표: 교시 구분 없이 과목별 열 반복 (기존 다과목 로직 그대로)
- 문항분석표: 교시·과목 드롭다운
- 개인별 성적표: 모든 교시 과목 통합 표시

---

## 🔄 영향 받는 로직 전체 점검

### 1. 세션 관리 (`session.js`)
- 세션 저장/로드 시 `periods` 배열 포함
- 기존 1교시 세션 마이그레이션 (자동으로 `periods=[{id:'p1', name:'1교시', images:기존이미지, ...}]`로 변환)

### 2. 이미지 관리 (`imageManager.js`)
- 현재 교시의 이미지만 리스트에 표시
- 업로드 시 `periodId` 자동 할당
- 이미지 이동/복사 기능

### 3. ROI 편집 (`canvas.js`, `ui.js`)
- 이미지에 ROI 그리면 그 이미지의 `periodId` 교시 설정에 저장
- 같은 교시 내 이미지 간 ROI 공유 (기존 동작 유지)
- 교시 전환 시 ROI 초기화되지 않음 (교시별 독립 보관)

### 4. 답안키 (`grading.js`)
- `App.state.answerKey` → `session.periods[p].answerKey`
- 답안키 편집 UI가 현재 교시의 답안키를 편집

### 5. 분석 파이프라인 (`canvas.js`, `batch.js`)
- 현재 교시 이미지만 분석
- 또는 전체 일괄 분석 (모든 교시 순회)

### 6. 채점 엔진 (`grading.js`)
- 교시별 답안키로 교시별 이미지 채점
- 결과는 이미지 단위로 `gradeResult`에 저장 (기존과 동일)

### 7. 집계 (`scoring.js`)
- **가장 큰 변화**: `collectData()`가 여러 교시의 이미지를 **수험번호로 매칭**해서 한 학생 row로 합침
- 학생 매칭 알고리즘 (교시 간):
  1. 수험번호 일치
  2. (수험번호 없을 때) 전화번호 일치
  3. (둘 다 없을 때) 이름 일치
  4. 매칭 실패 시 해당 교시 과목은 `null` (미응시 처리)
- 과목별 분석은 기존 로직 그대로 (subjects dict에 과목이 어느 교시에서 왔는지 관계없이 합산)

### 8. 성적표 (`scoring.js`)
- 성적일람표: 그대로 작동 (과목별 열 반복 + 총점)
- 개인별 성적표: 과목별 카드에 교시 표기 추가 ("국어 (1교시)")
- 문항분석표: 과목 드롭다운에 "모든 교시" 또는 교시별 선택 옵션

### 9. CSV (`scoring.js`, `subjectManager.js`)
- 시험인원 CSV: 세션 공유 (변화 없음)
- 과목/정답 CSV: 교시별로 로드할 수 있어야 함 (현재 교시의 과목만 편집)
- OMR 결과 CSV: 교시 정보 포함 or 과목만

### 10. 시험관리 모달 (`subjectManager.js`)
- 과목 관리 UI에 "현재 교시" 표시
- 또는 교시별로 과목 섹션 분리

---

## 🔨 구현 로드맵

### Phase 1 — 데이터 모델 + 마이그레이션
1. `App.state.session.periods` 배열 도입
2. 기존 세션 자동 마이그레이션 (1교시만 있는 형태로)
3. 현재 교시 전환 상태 관리 (`currentPeriodId`)

### Phase 2 — 교시 관리 UI
4. 상단에 교시 탭/드롭다운 추가
5. 교시 추가/삭제/이름변경
6. 교시 전환 시 이미지 목록·ROI·답안키 전환

### Phase 3 — ROI/답안키 교시별 분리
7. ROI 편집이 현재 교시에만 영향
8. 답안키 편집이 현재 교시에만 영향
9. 교시 간 ROI 템플릿 복사 기능

### Phase 4 — 분석/채점 교시별 독립
10. 분석 루프가 현재 교시 이미지만 처리
11. "전체 교시 일괄 분석/채점" 버튼

### Phase 5 — collectData 교시 간 통합 (핵심)
12. 교시별 이미지에서 추출한 데이터를 학생별로 병합
13. 과목 정보에 `periodId` 부착
14. 결시 처리

### Phase 6 — 성적표 + CSV
15. 성적일람표에 교시/과목 표기
16. 개인별 성적표 교시 표시
17. CSV 다운로드 교시 정보 포함

### Phase 7 — 세션 저장/로드
18. periods 배열 포함 저장
19. 기존 포맷 하위호환 로드

---

## ✅ 확정 사항 (피드백 반영 최종본)

### 1. 교시별 같은 과목명 처리 → **합산**
같은 과목명이면 교시가 달라도 합산. 분리하고 싶으면 사용자가 이름을 다르게 설정.

### 2. 교시 이름 → **1교시/2교시/... 자동 + 사용자 편집 가능**
더블클릭으로 이름 변경.

### 3. 이미지-교시 관계 → **이미지는 한 교시에 소속 (이동 가능)**
교시 미생성 시 자동 1교시 1개 생성.

### 4. 학생 매칭 → **수험번호 > 전화번호 > 이름**
- 필드 중 일부 누락은 오류 아님 (다른 필드로 시도)
- **매칭 정보 전부 누락** 시 채점 탭 진입 시 한 번에 모아 다이얼로그 (건너뛸지 물어봄)

### 5. 교시 삭제 → **이미지는 살아있고 '미할당' 상태로 이동, 채점 제외**
삭제 전 alert로 경고.

### 6. 성적일람표 → **과목명만 표기, 툴팁에 교시 노출**

### 7. 시험관리 모달 → **교시 구분 없음 (과목명 기준 매칭)**
현재 UI 유지.

### 8. 기존 세션 → **자동 1교시 1개로 마이그레이션**
과거 테스트 데이터는 무시.

### 9. 우선순위 → **가장 빠르고 안전하게, 단계별 커밋으로 롤백 가능**

---

## ➕ 추가 확정 사항

### 10. UI 레이아웃
```
┌─ 메인 헤더 ─────────────────────────────────────────┐
│ [좌측]       [시험 이름 | 시험 일자 — 중앙]    [우측]│
└─────────────────────────────────────────────────────┘
┌────────────────────────────────────────────────┐
│  [분석 탭]  [채점 탭]               ← 상단 모드
├────────────────────────────────────────────────┤
│  [1교시] [2교시] [3교시] [+]        ← 교시 탭 (항상 표시)
├────────────────────────────────────────────────┤
│  🔍 이미지 검색...                  ← 이미지 목록
│  • 이미지1.jpg (현재 교시의 것만)
└────────────────────────────────────────────────┘
```

### 11. 교시 탭 인터랙션
- **더블클릭** → 이름 편집
- **X 버튼** → 삭제 (이미지는 미할당으로, 채점 제외 alert)
- **드래그** → 순서 변경
- **+ 탭** → 자동 "N교시" 추가

### 12. 이미지 이동
- **우클릭 → "다른 교시로 이동" → 대상 교시 선택**
- **Shift 연속 다중선택 + Ctrl 개별 다중선택** 지원

### 13. 신규 세션 생성 UI
- "세션 이름" 입력 → **"시험 이름"** 필드로 변경
- **"시험 일자"** 필드 추가 (오늘 자동 + 편집 가능)
- 세션 식별 키 = `시험이름_YYYY-MM-DD` 조합

### 14. 파일명 재명명 (세션 저장 시)
매칭된 이미지는 `{이름}_{연락처}_{수험번호}_{생년월일}_원본파일명` 으로 저장.
- 매칭 필드가 일부만 있으면 있는 것만 포함
- 매칭 실패 이미지는 원본 파일명 유지
- 구분자는 `_`

### 15. 삭제된 이미지 세션 저장 (버그 수정 포함)
현재 `deletedImages`는 세션 저장 대상에서 제외되어 있어 복구 불가.
→ **실제 이미지 데이터까지 세션에 저장되도록 수정**.

---

## 🛠 구현 순서 (안전 + 빠름)

각 단계마다 **독립 커밋**. 문제 시 해당 단계만 `git revert` 가능.

### Step 0 — 독립 사전작업 (위험도 낮음)
1. **시험 이름/일자 입력 + 헤더 중앙 표시** (신규 세션 모달 + CSS)
2. **삭제된 이미지 세션 저장 버그 수정** (session.js)
3. **파일명 재명명** (세션 저장 시 매칭값 prefix 부여)

### Step 1 — 데이터 모델
4. `App.state.periods` 배열 도입 + 자동 1교시 생성 + 이미지 `periodId` 부착
5. 기존 구조 접근 코드에 호환 레이어 (getImages() 등 헬퍼)

### Step 2 — 교시 탭 UI
6. 교시 탭 렌더링 (분석/채점 탭 밑, 이미지 목록 위)
7. +탭으로 교시 추가, 더블클릭 이름변경, X 삭제, 드래그 순서변경

### Step 3 — 교시별 독립
8. 이미지 목록이 현재 교시 이미지만 표시
9. ROI 편집/답안키가 현재 교시에만 반영
10. 분석/채점 루프가 현재 교시만 대상 (+ 전체 교시 일괄 버튼)

### Step 4 — 이미지 이동
11. 우클릭 컨텍스트 메뉴: "다른 교시로 이동"
12. Shift/Ctrl 다중선택 기능

### Step 5 — 채점 통합 (핵심)
13. `collectData`가 모든 교시 순회 → 학생별 병합 (수험번호 > 전화 > 이름)
14. 매칭 실패 케이스 다이얼로그 (채점 탭 진입 시 한 번에)
15. 성적표 툴팁에 교시 표기

### Step 6 — 세션 저장/로드
16. `periods` 배열 저장
17. `시험이름_날짜` 키로 저장
18. 로드 시 `periods` 복원 (자동 마이그레이션 포함)

---

## 🎤 다음 단계

위 "⚠ 확정 필요 사항" 9개에 대해 답변 주시면 그에 맞춰 구현 시작.
