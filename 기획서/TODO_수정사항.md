# 수정사항 TODO (계속 갱신)

6. omr 블롭을 판단함에 있어서, 학생이 수정테이프로 지워서, 블롭이 아예 없어진경우, 학생이 정해진 버블에만 칠한게 아니라 과도하게 크게 칠해서, 기존 블롭을 찾는데 문제가 있는 경우들이 있음. 이걸 어떻게 하면 좋을지 기획필요함. (🕐 대기 — 별도 기획 논의 필요)

> 새 요청은 채팅으로 말씀하시면 여기 표에 "🕐 대기"로 추가합니다. 상태는 🕐 대기 → 🔧 진행중 → ✅ 완료 순으로 바뀝니다.
> 이 문서는 v2 개발용 폴더(`NEW_OMRGRADING_성능개선`) 기준입니다.

---

## 표

| 상태 | 날짜 | 내용 | 비고 |
|---|---|---|---|
| ✅ 완료 | 2026-07-08 | 교정탭 썸네일이 이미지 전체 해상도를 재처리해서 느림 (원인 C) | `canvas.js`에 `_getIntensifiedCrop` 추가, 크롭 후 처리로 변경 |
| ✅ 완료 | 2026-07-08 | 교정탭 지연로드 시 전체 재렌더 + 중복 로드 (원인 D) | `correction.js` 카드 단위 교체, `imageManager.js` 로딩 Promise dedupe |
| ✅ 완료 | 2026-07-08 | 일괄분석(배치) 중 이미지 비트맵 미해제로 메모리 누적 (원인 A) | `batch.js`에 최근 5장만 유지하는 순차 해제 추가 |
| ✅ 완료 | 2026-07-08 | 결과표(채점 탭)가 조작마다 전체 재렌더 (원인 B) | `scoring.js` OMR 결과표 페이지네이션(150행) 추가 |
| ✅ 완료 | 2026-07-08 | 세션 저장 시 전체 이미지 base64를 한 번에 IPC 전송 → 메모리 폭증/크래시 위험 (원인 E) | `session.js`/`main.js` 30장 단위 청크 저장으로 변경 |
| ✅ 완료 | 2026-07-08 | 세션 부분 저장 시 미변경 이미지 파일이 삭제되는 버그 (원인 F) | 정리(cleanup) 판단 기준을 "전체 파일 목록"으로 분리 |
| ✅ 완료 | 2026-07-08 | 시험인원 CSV 불러오기 시 생년월일이 yyyymm으로 잘림 (일→소실) | `subjectManager.js` `_normalizeField`: 8자리 입력 시 뒤 6자리(연2+월+일) 사용하도록 수정 |
| ✅ 완료 | 2026-07-09 | 배포 준비 — 앱 식별자를 v1과 분리 | `package.json`(name/appId/productName), `main.js`, `index.html`을 `_v2`로 변경 |
| ✅ 완료 | 2026-07-09 | 세션 불러오기 시 이미지 동시 로딩 무제한 → 대량 세션에서 메모리 폭증/크래시 위험 | `session.js` `loadSession()`을 업로드와 동일하게 `PARALLEL=4` 큐 방식으로 변경 |
| ✅ 완료 | 2026-07-09 | 빈칸(-) 수정 시 이미지에 빈칸이 2개 이상이면 마지막 항목에서 무한루프 걸림 | `ui.js` 오류 셀 탐색(`onCellInput`/`moveToNextImageError`)이 DOM 클래스만 보고 판단해 이미 교정된 셀을 재탐지하던 버그 — `grid-cell-corrected` 클래스 제외 조건 추가 |
| ✅ 완료 | 2026-07-09 | 일괄처리(배치) 중 취소 기능 없음 | `batch.js` 모달에 취소 버튼 추가 — 진행 중인 이미지까지 처리 후 중단, 기존 결과는 유지 |
| ✅ 완료 | 2026-07-09 | 이미지목록창 크기 고정 (330px) | 목록/캔버스 사이에 드래그 리사이저 추가, 너비를 localStorage에 저장 |
| ✅ 완료 | 2026-07-09 | 채점/결과 탭에서 항목 클릭해도 분석 탭으로 이동 안 됨 | `scoring.js` OMR결과표·성적일람표 행에 클릭 이동 추가 (다교시 세션은 교시도 자동 전환) |
| ✅ 완료 | 2026-07-09 | 채점/결과 탭을 새창에서 볼 수 없음 | Electron 보조 창(`report-window.html`) 추가 — 메인 창이 결과표를 실시간으로 미러링, 새창에서 행 클릭 시 메인 창이 분석 탭으로 이동 (보기 전용, 데이터는 메인 창에서만 관리) |
| ✅ 완료 | 2026-07-09 | 분석 탭 이미지목록에서 이름 미매칭 학생만 따로 보기 불가 | `imageManager.js`에 "미매칭" 필터 탭 추가 (`applyPhonePrefix`에서 매칭 여부를 `_nameMatched`로 기록) |
| 🕐 대기 (보류) | 2026-07-08 | 오류문항 추가 재시도 대상이 원본(300장)보다 dev(959장)에서 훨씬 많이 잡힘 | 코드 추적으론 원인 미발견. 분석 로그(`OmrEngine._fileLog`) 확보 후 재조사 필요 |
| 참고 (미확정) | 2026-07-08 | 게시용 성적표(`publicReport.js`)도 대량 인원(962명)에서 결과표 탭처럼 느릴 수 있음 | 확정된 버그 아님. 실제로 대량 인원으로 뽑아보고 느리면 그때 처리 |
